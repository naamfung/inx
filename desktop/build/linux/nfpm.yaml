# nfpm config for the Linux .deb package. scripts/desktop-build.sh's linux branch
# runs `nfpm package` after `wails build`, from the desktop/ dir (so the src paths
# below are relative to desktop/). Portable clients keep using the .tar.gz under
# platforms[]; this .deb is the native package channel (native_packages) and also
# remains a human-download artifact.
#
# $DEB_VERSION and $DEB_ARCH are exported by desktop-build.sh. DEB_VERSION follows
# Debian version ordering: stable X.Y.Z, prereleases as X.Y.Z~rc.N / X.Y.Z~canary.N
# so they sort below the matching stable package. DEB_ARCH is the Go arch name,
# which already matches Debian's (amd64, arm64).
name: "inx-desktop"
arch: "${DEB_ARCH}"
platform: "linux"
version: "${DEB_VERSION}"
section: "utils"
priority: "optional"
maintainer: "esengine <esengine@users.noreply.github.com>"
description: |
  Inx desktop — a Wails shell around the Go kernel.
vendor: "Inx Contributors"
homepage: "https://github.com/esengine/DeepSeek-Inx"
license: "MIT"
scripts:
  postinstall: ./build/linux/postinstall.sh
# Runtime libraries the WebKitGTK 4.1 build links against (-tags webkit2_41); apt
# pulls them in on install. These package names ship from Ubuntu 22.04 / Debian 12 on.
# pkexec is required for the in-app Polkit authorization update path.
depends:
  - libgtk-3-0
  - libwebkit2gtk-4.1-0
  - pkexec
contents:
  - src: ./build/bin/inx-desktop
    dst: /usr/bin/inx-desktop
    file_info:
      mode: 0755
  - src: ./build/bin/inx-launcher
    dst: /usr/bin/inx-launcher
    file_info:
      mode: 0755
  - src: ./build/bin/inx
    dst: /usr/bin/inx
    file_info:
      mode: 0755
  - src: ./build/bin/inx-update-helper
    dst: /usr/lib/inx/inx-update-helper
    file_info:
      mode: 0755
  - src: ./build/linux/io.inx.desktop.update.policy
    dst: /usr/share/polkit-1/actions/io.inx.desktop.update.policy
    file_info:
      mode: 0644
  - src: ./build/linux/inx.desktop
    dst: /usr/share/applications/inx.desktop
    file_info:
      mode: 0644
  # appicon.png is 1024x1024 — not a standard hicolor size dir, so install it under
  # the size-agnostic pixmaps path that the .desktop's `Icon=inx-desktop` finds.
  - src: ./build/appicon.png
    dst: /usr/share/pixmaps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/16x16/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/16x16/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/24x24/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/24x24/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/32x32/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/32x32/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/48x48/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/48x48/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/64x64/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/64x64/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/128x128/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/128x128/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/256x256/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/256x256/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/512x512/apps/inx-desktop.png
    dst: /usr/share/icons/hicolor/512x512/apps/inx-desktop.png
    file_info:
      mode: 0644
  - src: ./build/linux/icons/hicolor/scalable/apps/inx-desktop.svg
    dst: /usr/share/icons/hicolor/scalable/apps/inx-desktop.svg
    file_info:
      mode: 0644
