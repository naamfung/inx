{
  "apiVersion": "inx.io/plugin/v1",
  "name": "starter-extension",
  "version": "0.1.0",
  "description": "Minimal Inx Extension Protocol v1 sidecar",
  "contributes": {},
  "runtime": {
    "command": "${INX_PLUGIN_ROOT}/bin/starter-extension.exe",
    "args": [],
    "env": {},
    "required": false,
    "priority": 0,
    "intercepts": ["input.receive"],
    "replaces": [],
    "capabilities": ["interceptors"]
  }
}
